import math;

public class MyProgram; {
	public static int value;

	private void counter (int counts) {
		int i;

		i = 0;		
		if (counts != 0) 
			while i < counts loop i = i - 1;
		print(i * 5);
		return i;
	}
}
